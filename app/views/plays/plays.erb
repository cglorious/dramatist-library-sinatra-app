<!doctype html>
<html>
  <head>
    <title>Plays</title>
    <style>
      #plays-welcome{
        color: #C0C0C0;
        text-align: center;
      }
      hr.solid {
        border-top: 1px solid #bbb;
      }
      a.play-link:link {
        color: #0277bd;
        background-color: transparent;
      }
      a.play-link:visited {
        color: #0277bd;
        background-color: transparent;
      }
    .play-btns {
      background-color: #ADD8E6;
      padding: 2px 12px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 16px;
      margin: 4px 2px;
      cursor: pointer;
      border-radius: 4x;
      transition-duration: 0.4s;
    }
    .play-btns:hover {
      background-color: #0277bd;
      color: white;
    }
    #new-menu {
      text-align: center;
    }
    .menu-btns {
      background-color: #C0C0C0;
      padding: 8px 18px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 16px;
      margin: 4px 2px;
      cursor: pointer;
      border-radius: 4x;
      transition-duration: 0.4s;
    }
    .menu-btns:hover {
      background-color: #0277bd;
      color: white;
    }
    </style>
  </head>
  <body>

    <h2>Welcome to your library.</h2>
    <h4 id="plays-welcome"> Hello, <%= current_user.name %>! <h4>
    <br>
    <!-- edit bio option -->
    <% if current_user.bio == nil || "" %>
      <h4> Let's get acquainted! </h4>
      <p>It looks like you don't have a biography.
      <br>
      Click <em><strong>Add Bio</strong></em> to add your biography.</p>
      <p><a class="button" href="/bio"><button class="play-btns">Add Bio</button></a></p>
    <% else %>
      <h4> All about you. </h4>
      <br>
      <p><%= current_user.bio %></p>
    <% end %>
    <br>
    <% if current_user.plays.count == 0 %>
      <h4>Oh snap!</h4>
      <p> It looks like you don't have any plays.
      <br>
      Click <em><strong>Add Play</strong></em> to add a new play. </p>
    <% else %>
      <h4> Here are your plays, <%= current_user.name %>.</h4>
      <ul>
        <% current_user.plays.each do |play| %>
          <a class= "play-link" href="/plays/<%= play.id %>"><li><%= play.name %></li></a>
        <% end %>
      </ul>
    <% end %>

    <p><a class="button" href="/plays/new"><button class="play-btns">Add Play</button></a></p>
    <br>
    <hr class="solid">
    <br>
    <h4> All Plays by Title </h4>
      <br>
    <ul>
      <!-- make list alphabetical -->
      <% Play.all.each do |play| %>
        <!-- unable to route to play -->
        <a class= "play-link" href="/plays/<%= play.id %>"><li><%= play.name %></li></a>
        <br>
        <% end %>
    </ul>

    <div id="new-menu">
      <hr class="solid">
      <br>
      <a class="button" href="/logout"><button class="menu-btns">Logout</button></a>
    </div>

  </body>
</html>
